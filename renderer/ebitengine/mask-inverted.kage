//kage:unit pixels

package main

func Fragment(dstPos vec4, srcPos vec2, col vec4) vec4 {
    maskBuffer := imageSrc0At(srcPos)
    frameBuffer := imageSrc1At(srcPos)

    maskVal := maskBuffer.r + maskBuffer.g + maskBuffer.b + maskBuffer.a

    return frameBuffer * (1.0 - maskVal)
}
